services:
  events_subscriber: #creating a eventsubscriber
    class: '\Drupal\task_head\EventSubscriber\HeaderRemover' #where the file is present
    tags:
      - { name: 'event_subscriber' } # to use event subscriber
